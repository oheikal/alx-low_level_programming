#include <stdio.h>
/**
  *main Entry - Point
  *
  * Return 0(Success)
  */
int main(void)
{
	int alphabet = 97;
	for (alphabet; alphabet < 123; alphabet++)
	{
		putchar(alphabet);
		putchar('\n');
	}
	return (0);
}
